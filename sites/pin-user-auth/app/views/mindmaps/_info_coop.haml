-cooperate_edit_actors = @mindmap.cooperate_edit_email_actors
-cooperate_view_actors = @mindmap.cooperate_view_email_actors

-if(!cooperate_edit_actors.blank?)
  .coop.darkbg
    由以下用户共同编辑：
    .users
      -cooperate_edit_actors.each do |actor|
        .u
          .avatar
            =avatar(actor.email,:tiny)
          .name=actor.name

-if(!cooperate_view_actors.blank? && @mindmap.private?)
  .coop.darkbg
    共同查看者：
    .users
      -cooperate_view_actors.each do |actor|
        .u
          .avatar
            =avatar(actor.email,:tiny)
          .name=actor.name