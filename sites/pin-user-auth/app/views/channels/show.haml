-require_css 'views/users',true

-content_for :javascripts do
  = javascript_include_tag "views/feed_operation"

.current-channel
  .title
    当前频道 -
    -if @current_channel == 'none'
      默认频道
    -else
      =@current_channel.name
      =@current_channel.kind

.grid_14.alpha
  -if !@channel.blank?
    -case @channel.kind
      -when Channel::KIND_BLOG
        =render :partial=>'channels/channel_blog',:locals=>{:feed=>@feeds}
      -when Channel::KIND_INTERVIEW
        =render :partial=>'channels/channel_interview',:locals=>{:feed=>@feeds}
      -else
        .form
          =render :partial=>'index/homepage/feeds/channel_feed_form'
        .newsfeed
          =render :partial=>'index/homepage/feeds/new_feeds',:locals=>{:newsfeeds=>@feeds}
  -else
    .form
      =render :partial=>'index/homepage/feeds/channel_feed_form'
    .newsfeed
      =render :partial=>'index/homepage/feeds/new_feeds',:locals=>{:newsfeeds=>@feeds}