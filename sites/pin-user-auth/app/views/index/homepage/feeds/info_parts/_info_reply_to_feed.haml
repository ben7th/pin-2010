-if(!feed.reply_to.blank?)
  .reply-to-feed.darkbg.quote
    -rf = feed.replied_feed
    .data
      -if rf.nil?
        %span.quiet 被回应的话题已删除
      -else
        -rc = rf.creator
        =link_to rc.name,rc
        =feed_content(rf)