.clearfix
  .member-logo
    =avatar concat.email,:tiny
  .member-email
    -actor = EmailActor.new(concat.email)
    =actor.email_name
  .delete
    =link_to_remote '删除',:url=>"/concats/#{concat.id}",:method=>'DELETE'