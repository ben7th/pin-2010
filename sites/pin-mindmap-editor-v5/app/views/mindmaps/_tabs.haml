%ul
  %li
    -if logged_in?
      =tabs_link_to "我的导图","/users/#{current_user.id}/mindmaps"
      =tabs_link_to "协同导图","/users/#{current_user.id}/cooperate"
      =tabs_link_to "个人趋势与统计","/users/#{current_user.id}/tendency"

    -if @user && @user != current_user
      =tabs_link_to "#{@user.name}的公开导图","/users/#{@user.id}/mindmaps"

    =tabs_link_to "浏览公开导图","/mindmaps"
