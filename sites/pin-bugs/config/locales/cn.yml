cn:
  activerecord:
    attributes:
      bug:
        attachment: 附件（可选）
      comment:
        content: 评论正文
      api_help:
        title: API标题
        description: API描述
        url: 请求URL
        format: 支持格式
        http_method: HTTP请求方式
        example: 是否需要登录
        params: 请求参数
        memo: 备注
        example: 范例
        result: 结果范例
      api_param:
        key: 参数名
        kind: 类型和取值范围
        description: 说明
        required: 必选
    errors:
      models:
        bug:
          attributes:
            content:
              blank: 内容不能为空