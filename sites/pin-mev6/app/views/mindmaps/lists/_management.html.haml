.page-user-mindmaps
  .mindmap
    =render 'index/parts/manage_bar'
  -if mindmaps.blank?
    .mindmap.empty 目前还没有思维导图
  -else
    -mindmaps.each do |mindmap|
      .mindmap{:'data-id'=>mindmap.id}
        .m
          .title=link_to mindmap.title,"/mindmaps/#{mindmap.id}/info"
          -size = mindmap.thumb_image_true_size
          .thumb{:style=>"width:#{size[:width]}px;height:#{size[:height]}px;"}
            -link_to "/mindmaps/#{mindmap.id}/info" do
              =mindmap_thumb(mindmap)

          .foot-misc
            .ops
              -if mindmap.user==current_user
                =link_to '编辑',"/mindmaps/#{mindmap.id}/edit",:target=>'_blank',:class=>'edit'
                =link_to '删除','javascript:;',:class=>'op delete'

            .rank
              =mindmap.rank_value
            .creator
              .avatar
                =avatar mindmap.user